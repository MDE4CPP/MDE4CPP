// DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN
// @generator: ecore4cpp::generator::main::generateBuildFile
plugins{
	id "tui.sse.mde4cpp.MDE4CPPGenerate" version "0.6"	
}

import org.gradle.internal.os.OperatingSystem;

description 'Build task for project: ecoreAnnotationsExample'


task runCommandCompileEcoreAnnotationsExample(type:Exec) {
	workingDir project.projectDir
	def rootPath = project.rootDir.toPath()
	def projectPath = project.projectDir.toPath()
	def relativeToRoot = rootPath.relativize(projectPath)
	def depth = relativeToRoot.nameCount
	def gradlewRelPath = '../' * depth + 'application/tools/gradlew'
	if (OperatingSystem.current().isWindows()) {
		def gradlewPathWin = gradlewRelPath.replace('/', '\\')
		commandLine 'cmd', '/c', gradlewPathWin + '.bat src_gen:compileEcoreAnnotationsExampleSrc'
	} else {
		commandLine '/bin/sh', '-c', gradlewRelPath + ' src_gen:compileEcoreAnnotationsExampleSrc'
	}
	dependsOn 'generateEcoreAnnotationsExample'
	dependsOn ':src:ecore:buildEcore'
	dependsOn ':src:uml:types:buildTypes'
		def allDepOutputs = files()
		getRootProject().getTasksByName('buildEcore', true).each { task ->
			allDepOutputs.from(task.outputs.files)
		}
		getRootProject().getTasksByName('buildTypes', true).each { task ->
			allDepOutputs.from(task.outputs.files)
		}
		inputs.files(allDepOutputs)
		mustRunAfter = [
			'generateEcoreAnnotationsExample'
			, getRootProject().getTasksByName('compileEcore', true)
			, getRootProject().getTasksByName('buildEcore', true)
			, getRootProject().getTasksByName('compileTypes', true)
			, getRootProject().getTasksByName('buildTypes', true)
		]
	doFirst {
		def mde4cppHome = System.getenv('MDE4CPP_HOME')
		if (mde4cppHome && OperatingSystem.current().isLinux()) {
			def ecoreLibFile = new File(mde4cppHome, 'application/bin/ecore.so')
			if (!ecoreLibFile.exists()) {
				sleep(2000)
				if (!ecoreLibFile.exists()) {
					def taskPath = ':src:ecore:buildEcore'
					throw new GradleException("Dependency library file does not exist: " + ecoreLibFile.absolutePath + ". Task " + taskPath + " must complete first.")
				}
			}
			def typesLibFile = new File(mde4cppHome, 'application/bin/types.so')
			if (!typesLibFile.exists()) {
				sleep(2000)
				if (!typesLibFile.exists()) {
					def taskPath = ':src:uml:types:buildTypes'
					throw new GradleException("Dependency library file does not exist: " + typesLibFile.absolutePath + ". Task " + taskPath + " must complete first.")
				}
			}
		}
	}
}

task runCommandGenerateEcoreAnnotationsExample(type: tui.sse.mde4cpp.MDE4CPPGenerate) {
	def modelPath = file('.' + File.separator + 'model' + File.separator + 'ecoreAnnotationsExample.ecore')
	modelFilePath = modelPath.absolutePath
}

task runCommandCompileApplicationForEcoreAnnotationsExample(type:Exec) {
	workingDir project.projectDir
	def rootPath = project.rootDir.toPath()
	def projectPath = project.projectDir.toPath()
	def relativeToRoot = rootPath.relativize(projectPath)
	def depth = relativeToRoot.nameCount
	def gradlewRelPath = '../' * depth + 'application/tools/gradlew'
	if (OperatingSystem.current().isWindows()) {
		def gradlewPathWin = gradlewRelPath.replace('/', '\\')
		commandLine 'cmd', '/c', gradlewPathWin + '.bat application:compileApplicationForEcoreAnnotationsExampleSrc'
	} else {
		commandLine '/bin/sh', '-c', gradlewRelPath + ' application:compileApplicationForEcoreAnnotationsExampleSrc'
	}
	dependsOn 'compileEcoreAnnotationsExample'
}

task generateEcoreAnnotationsExample() {
	group 'ecoreAnnotationsExample'	
	description 'generate C++ code of ecoreAnnotationsExample.ecore model ecoreAnnotationsExample' 
 
	def buildFile = file('model'+ File.separator +'build.gradle')
	if(!buildFile.exists())
	{
		dependsOn 'runCommandGenerateEcoreAnnotationsExample'
	}
	else
	{
		dependsOn 'model:generateEcoreAnnotationsExampleModel'
	}
}

task compileEcoreAnnotationsExample() {
	group 'ecoreAnnotationsExample'
	description 'compile ecoreAnnotationsExample' 

	dependsOn 'generateEcoreAnnotationsExample'
	mustRunAfter = ["generateEcoreAnnotationsExample"]
	
	def buildFile = file('src_gen'+ File.separator + 'build.gradle')
	
	if(buildFile.exists()) {
		dependsOn 'src_gen:compileEcoreAnnotationsExampleSrc'
	} else {
		dependsOn 'runCommandCompileEcoreAnnotationsExample'
	}
	
		mustRunAfter = [
			'generateEcoreAnnotationsExample'
			, getRootProject().getTasksByName('compileEcore', true)
			, getRootProject().getTasksByName('buildEcore', true)
			, getRootProject().getTasksByName('compileTypes', true)
			, getRootProject().getTasksByName('buildTypes', true)
		]
}

task compileApplicationForEcoreAnnotationsExample{
	group 'ecoreAnnotationsExample'
	description 'compile ApplicationForecoreAnnotationsExample'

	dependsOn 'runCommandCompileApplicationForEcoreAnnotationsExample'
	dependsOn 'compileEcoreAnnotationsExample'
	mustRunAfter = ["compileEcoreAnnotationsExample"]
}

task buildEcoreAnnotationsExample() {
	group 'ecoreAnnotationsExample'
	description 'build ecoreAnnotationsExample'

	dependsOn 'generateEcoreAnnotationsExample'
	dependsOn 'compileEcoreAnnotationsExample'
	dependsOn 'compileApplicationForEcoreAnnotationsExample'

	dependsOn getRootProject().getTasksByName('buildEcore', true)
	dependsOn getRootProject().getTasksByName('buildTypes', true)
}
