// DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN
// @generator: ecore4cpp::generator::main::generateBuildFile
plugins{
	id "tui.sse.mde4cpp.MDE4CPPGenerate" version "0.6"	
}

import org.gradle.internal.os.OperatingSystem;

description 'Build task for project: copyTest'


task runCommandCompileCopyTest(type:Exec) {
	workingDir project.projectDir
	def rootPath = project.rootDir.toPath()
	def projectPath = project.projectDir.toPath()
	def relativeToRoot = rootPath.relativize(projectPath)
	def depth = relativeToRoot.nameCount
	def gradlewRelPath = '../' * depth + 'application/tools/gradlew'
	if (OperatingSystem.current().isWindows()) {
		def gradlewPathWin = gradlewRelPath.replace('/', '\\')
		commandLine 'cmd', '/c', gradlewPathWin + '.bat src_gen:compileCopyTestSrc'
	} else {
		commandLine '/bin/sh', '-c', gradlewRelPath + ' src_gen:compileCopyTestSrc'
	}
	dependsOn 'generateCopyTest'
	dependsOn ':src:ecore:buildEcore'
		def allDepOutputs = files()
		getRootProject().getTasksByName('buildEcore', true).each { task ->
			allDepOutputs.from(task.outputs.files)
		}
		inputs.files(allDepOutputs)
		mustRunAfter = [
			'generateCopyTest'
			, getRootProject().getTasksByName('compileEcore', true)
			, getRootProject().getTasksByName('buildEcore', true)
		]
	doFirst {
		def mde4cppHome = System.getenv('MDE4CPP_HOME')
		if (mde4cppHome && OperatingSystem.current().isLinux()) {
			def ecoreLibFile = new File(mde4cppHome, 'application/bin/ecore.so')
			if (!ecoreLibFile.exists()) {
				sleep(2000)
				if (!ecoreLibFile.exists()) {
					def taskPath = ':src:ecore:buildEcore'
					throw new GradleException("Dependency library file does not exist: " + ecoreLibFile.absolutePath + ". Task " + taskPath + " must complete first.")
				}
			}
		}
	}
}

task runCommandGenerateCopyTest(type: tui.sse.mde4cpp.MDE4CPPGenerate) {
	def modelPath = file('.' + File.separator + 'model' + File.separator + 'copyTest.ecore')
	modelFilePath = modelPath.absolutePath
}

task runCommandCompileApplicationForCopyTest(type:Exec) {
	workingDir project.projectDir
	def rootPath = project.rootDir.toPath()
	def projectPath = project.projectDir.toPath()
	def relativeToRoot = rootPath.relativize(projectPath)
	def depth = relativeToRoot.nameCount
	def gradlewRelPath = '../' * depth + 'application/tools/gradlew'
	if (OperatingSystem.current().isWindows()) {
		def gradlewPathWin = gradlewRelPath.replace('/', '\\')
		commandLine 'cmd', '/c', gradlewPathWin + '.bat application:compileApplicationForCopyTestSrc'
	} else {
		commandLine '/bin/sh', '-c', gradlewRelPath + ' application:compileApplicationForCopyTestSrc'
	}
	dependsOn 'compileCopyTest'
}

task generateCopyTest() {
	group 'copyTest'	
	description 'generate C++ code of copyTest.ecore model copyTest' 
 
	def buildFile = file('model'+ File.separator +'build.gradle')
	if(!buildFile.exists())
	{
		dependsOn 'runCommandGenerateCopyTest'
	}
	else
	{
		dependsOn 'model:generateCopyTestModel'
	}
}

task compileCopyTest() {
	group 'copyTest'
	description 'compile copyTest' 

	dependsOn 'generateCopyTest'
	mustRunAfter = ["generateCopyTest"]
	
	def buildFile = file('src_gen'+ File.separator + 'build.gradle')
	
	if(buildFile.exists()) {
		dependsOn 'src_gen:compileCopyTestSrc'
	} else {
		dependsOn 'runCommandCompileCopyTest'
	}
	
		mustRunAfter = [
			'generateCopyTest'
			, getRootProject().getTasksByName('compileEcore', true)
			, getRootProject().getTasksByName('buildEcore', true)
		]
}

task compileApplicationForCopyTest{
	group 'copyTest'
	description 'compile ApplicationForcopyTest'

	dependsOn 'runCommandCompileApplicationForCopyTest'
	dependsOn 'compileCopyTest'
	mustRunAfter = ["compileCopyTest"]
}

task buildCopyTest() {
	group 'copyTest'
	description 'build copyTest'

	dependsOn 'generateCopyTest'
	dependsOn 'compileCopyTest'
	dependsOn 'compileApplicationForCopyTest'

	dependsOn getRootProject().getTasksByName('buildEcore', true)
}
