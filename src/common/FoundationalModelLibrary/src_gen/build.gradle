// DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN
// @generator: fuml4cpp::generator::main::generateExecutionBuildFile
import tui.sse.mde4cpp.MDE4CPPCompile;
import org.gradle.internal.os.OperatingSystem;

description 'Compile tasks for project: FoundationalModelLibrary'

task compileFoundationalModelLibrary(type: tui.sse.mde4cpp.MDE4CPPCompile) {
	group 'FoundationalModelLibrary'
	description 'compile FoundationalModelLibrary'

	projectFolder = file('.' + File.separator + 'FoundationalModelLibrary')

	inputs.files(fileTree('.') {
        exclude 'FoundationalModelLibrary/.cmake/**'
        exclude 'FoundationalModelLibraryExec/.cmake/**'
    })

	if(!file('.' + File.separator + 'FoundationalModelLibrary' + File.separator + '.cmake' + File.separator).exists()) {
		outputs.upToDateWhen { false }
	}

	if(!file('.' + File.separator + 'FoundationalModelLibraryExec' + File.separator + '.cmake' + File.separator).exists()) {
		outputs.upToDateWhen { false }
	}

	if(OperatingSystem.current().isWindows()){
		if(project.hasProperty('RELEASE') && !project.property('RELEASE').equals('0')){outputs.file file([rootDir,'application','bin','FoundationalModelLibrary' +                '.dll'].join(File.separator))}
		if(project.hasProperty('DEBUG') && !project.property('DEBUG').equals('0'))  {outputs.file file([rootDir,'application','bin','FoundationalModelLibrary' +          'd' + '.dll'].join(File.separator))}
		if(project.hasProperty('RELEASE') && !project.property('RELEASE').equals('0')){outputs.file file([rootDir,'application','bin','FoundationalModelLibrary' + 'Exec' +       '.dll'].join(File.separator))}
		if(project.hasProperty('DEBUG') && !project.property('DEBUG').equals('0'))  {outputs.file file([rootDir,'application','bin','FoundationalModelLibrary' + 'Exec' + 'd' + '.dll'].join(File.separator))}
	}
	else if(OperatingSystem.current().isLinux()){
		if(project.hasProperty('RELEASE') && !project.property('RELEASE').equals('0')){outputs.file file([rootDir,'application','bin','FoundationalModelLibrary' +                '.so'].join(File.separator))}
		if(project.hasProperty('DEBUG') && !project.property('DEBUG').equals('0'))  {outputs.file file([rootDir,'application','bin','FoundationalModelLibrary' +          'd' + '.so'].join(File.separator))}
		if(project.hasProperty('RELEASE') && !project.property('RELEASE').equals('0')){outputs.file file([rootDir,'application','bin','FoundationalModelLibrary' + 'Exec' +       '.so'].join(File.separator))}
		if(project.hasProperty('DEBUG') && !project.property('DEBUG').equals('0'))  {outputs.file file([rootDir,'application','bin','FoundationalModelLibrary' + 'Exec' + 'd' + '.so'].join(File.separator))}
	}
	else if(OperatingSystem.current().isMacOsX()){
		if(project.hasProperty('RELEASE') && !project.property('RELEASE').equals('0')){outputs.file file([rootDir,'application','bin','FoundationalModelLibrary' +                '.dylib'].join(File.separator))}
		if(project.hasProperty('DEBUG') && !project.property('DEBUG').equals('0'))  {outputs.file file([rootDir,'application','bin','FoundationalModelLibrary' +          'd' + '.dylib'].join(File.separator))}
		if(project.hasProperty('RELEASE') && !project.property('RELEASE').equals('0')){outputs.file file([rootDir,'application','bin','FoundationalModelLibrary' + 'Exec' +       '.dylib'].join(File.separator))}
		if(project.hasProperty('DEBUG') && !project.property('DEBUG').equals('0'))  {outputs.file file([rootDir,'application','bin','FoundationalModelLibrary' + 'Exec' + 'd' + '.dylib'].join(File.separator))}
	}
	else {
	    //not supported os
	}

	outputs.files(fileTree([rootDir,'application','include','FoundationalModelLibrary'].join(File.separator)))
	outputs.files(fileTree([rootDir,'application','include','FoundationalModelLibraryExec'].join(File.separator)))
	outputs.dir([rootDir,'application','include','FoundationalModelLibrary'].join(File.separator))
	outputs.dir([rootDir,'application','include','FoundationalModelLibraryExec'].join(File.separator))
	
	dependsOn getRootProject().getTasksByName('deliverBasicInterfaces', true)


	// dependency to model 'fUML'
	def fUML = getRootProject().getTasksByName('compileFUML', true)
	dependsOn fUML
	inputs.files(fUML.outputs)

	// dependency to model 'ecoreReflection'
	def ecoreReflection = getRootProject().getTasksByName('compileEcoreReflection', true)
	dependsOn ecoreReflection
	inputs.files(ecoreReflection.outputs)

	// dependency to model 'uml'
	def uml = getRootProject().getTasksByName('compileUml', true)
	dependsOn uml
	inputs.files(uml.outputs)

	// dependency to model 'PSCS'
	def PSCS = getRootProject().getTasksByName('compilePSCS', true)
	dependsOn PSCS
	inputs.files(PSCS.outputs)

	// dependency to model 'primitivetypesReflection'
	def primitivetypesReflection = getRootProject().getTasksByName('compilePrimitivetypesReflection', true)
	dependsOn primitivetypesReflection
	inputs.files(primitivetypesReflection.outputs)

	// dependency to model 'ecore'
	def ecore = getRootProject().getTasksByName('compileEcore', true)
	dependsOn ecore
	inputs.files(ecore.outputs)

	// dependency to model 'umlReflection'
	def umlReflection = getRootProject().getTasksByName('compileUmlReflection', true)
	dependsOn umlReflection
	inputs.files(umlReflection.outputs)
}

task clean {
	// Extension of gradle built-in task:clean
	doLast {
		def dot_cmake = file('FoundationalModelLibrary/.cmake')
		if(dot_cmake.exists()){
			delete dot_cmake.absolutePath
			println "deleting: " + dot_cmake.absolutePath
		}

		def dot_cmakeExec = file('FoundationalModelLibraryExec/.cmake')
		if(dot_cmakeExec.exists()){
			delete dot_cmakeExec.absolutePath
			println "deleting: " + dot_cmakeExec.absolutePath
		}
		
		compileFoundationalModelLibrary.outputs.files.each{
			if(it.exists()){
				delete it.absolutePath
			    println "deleting: " + it
			}
  		}
	}
}
