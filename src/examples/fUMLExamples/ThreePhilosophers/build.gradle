// DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN
// @generator: fuml4cpp::generator::main::generateExecutionBuildFile
plugins{
	id "tui.sse.mde4cpp.MDE4CPPGenerate" version "0.6"	
}

import org.gradle.internal.os.OperatingSystem;

description 'Build tasks for project: philosophers'

task runCommandCompilePhilosophers(type:Exec) {
	workingDir project.projectDir
	def rootPath = project.rootDir.toPath()
	def projectPath = project.projectDir.toPath()
	def relativeToRoot = rootPath.relativize(projectPath)
	def depth = relativeToRoot.nameCount
	def gradlewRelPath = '../' * depth + 'application/tools/gradlew'
	if (OperatingSystem.current().isWindows()) {
		def gradlewPathWin = gradlewRelPath.replace('/', '\\')
		commandLine 'cmd', '/c', gradlewPathWin + '.bat src_gen:compilePhilosophersSrc'
	} else {
		commandLine '/bin/sh', '-c', gradlewRelPath + ' src_gen:compilePhilosophersSrc'
	}
	dependsOn 'generatePhilosophers'
}

task runCommandGeneratePhilosophers(type: tui.sse.mde4cpp.MDE4CPPGenerate) {
	def modelPath = file('.' + File.separator + 'model' + File.separator + 'Philosophers.uml')
	modelFilePath = modelPath.absolutePath
}

task generatePhilosophers() {
	group 'philosophers'
	description 'description generate C++ code of philosophers.uml model philosophers' 
 
	def buildFile = file('model/build.gradle')
	if(!buildFile.exists())
	{
		dependsOn 'runCommandGeneratePhilosophers'
	}
	else
	{
		dependsOn 'model:generatePhilosophersModel'
	}
}

task runCommandCompileApplicationForPhilosophers(type:Exec) {
	workingDir project.projectDir
	def rootPath = project.rootDir.toPath()
	def projectPath = project.projectDir.toPath()
	def relativeToRoot = rootPath.relativize(projectPath)
	def depth = relativeToRoot.nameCount
	def gradlewRelPath = '../' * depth + 'application/tools/gradlew'
	if (OperatingSystem.current().isWindows()) {
		def gradlewPathWin = gradlewRelPath.replace('/', '\\')
		commandLine 'cmd', '/c', gradlewPathWin + '.bat application:compileApplicationForPhilosophersSrc'
	} else {
		commandLine '/bin/sh', '-c', gradlewRelPath + ' application:compileApplicationForPhilosophersSrc'
	}
	dependsOn 'compilePhilosophers'
}

task compilePhilosophers {
	group 'philosophers'
	description 'compile philosophers'

	dependsOn 'generatePhilosophers'
	mustRunAfter = ["generatePhilosophers"]
	
	def buildFile = file('src_gen/build.gradle')
	
	if(buildFile.exists()) {
		dependsOn 'src_gen:compilePhilosophersSrc'
	} else {
		dependsOn 'runCommandCompilePhilosophers'
	}
		mustRunAfter = [
			"generatePhilosophers"
			, getRootProject().getTasksByName('compileFUML', true)
			, getRootProject().getTasksByName('buildFUML', true)
			, getRootProject().getTasksByName('compileEcoreReflection', true)
			, getRootProject().getTasksByName('buildEcoreReflection', true)
			, getRootProject().getTasksByName('compileUml', true)
			, getRootProject().getTasksByName('buildUml', true)
			, getRootProject().getTasksByName('compilePSCS', true)
			, getRootProject().getTasksByName('buildPSCS', true)
			, getRootProject().getTasksByName('compilePrimitivetypesReflection', true)
			, getRootProject().getTasksByName('buildPrimitivetypesReflection', true)
			, getRootProject().getTasksByName('compileEcore', true)
			, getRootProject().getTasksByName('buildEcore', true)
			, getRootProject().getTasksByName('compileUmlReflection', true)
			, getRootProject().getTasksByName('buildUmlReflection', true)
		]
}

task compileApplicationForPhilosophers{
	group 'philosophers'
	description 'compile ApplicationForphilosophers'

	dependsOn 'runCommandCompileApplicationForPhilosophers'
	dependsOn 'compilePhilosophers'
	mustRunAfter = ["compilePhilosophers"]
}

task buildPhilosophers() {
	group 'philosophers'
	description 'build philosophers'

	dependsOn 'generatePhilosophers'
	dependsOn 'compilePhilosophers'
	dependsOn 'compileApplicationForPhilosophers'
	dependsOn getRootProject().getTasksByName('buildFUML', true)
	dependsOn getRootProject().getTasksByName('buildEcoreReflection', true)
	dependsOn getRootProject().getTasksByName('buildUml', true)
	dependsOn getRootProject().getTasksByName('buildPSCS', true)
	dependsOn getRootProject().getTasksByName('buildPrimitivetypesReflection', true)
	dependsOn getRootProject().getTasksByName('buildEcore', true)
	dependsOn getRootProject().getTasksByName('buildUmlReflection', true)
}

