description 'Example collection'

task buildAllExamples(dependsOn: ['compileUMLBenchmark', 'compileEcoreBenchmark', 'compileMemoryBenchmarkEcore', 'fumlExample:Calculator:buildCalcModel']) {
	group 'Examples'
	description 'build all example applications'	
}

task compileEcoreBenchmark(dependsOn: ['compileEcoreBenchmarkDebug', 'compileEcoreBenchmarkRelease']) {
	group 'Examples'
	description 'compile EcoreBenchmark in debug and release'
}
task compileEcoreBenchmarkDebug {
	group 'Examples'
	description 'compile EcoreBenchmark in Debug'

	def folder = new File(file('./benchmarks/ecoreBenchmark/.cmake/Debug').absolutePath)
	if( !folder.exists() ) {
		folder.mkdirs()
	}

	doLast {
		exec {
			workingDir 'benchmarks/ecoreBenchmark/.cmake/Debug'
			commandLine 'cmd', '/c', 'cmake -G "MinGW Makefiles" -D CMAKE_BUILD_TYPE=Debug ' + file("./benchmarks/ecoreBenchmark").absolutePath
		}
		exec {
			workingDir 'benchmarks/ecoreBenchmark/.cmake/Debug'
			if (make_parallel_jobs)
			{			
				commandLine 'cmd', '/c', 'mingw32-make install -j' + make_parallel_jobs
			}
			else
			{
				commandLine 'cmd', '/c', 'mingw32-make install' 
			}
		}
	}
}
task compileEcoreBenchmarkRelease {
	group 'Examples'
	description 'compile EcoreBenchmark in Release'

	def folder = new File(file('./benchmarks/ecoreBenchmark/.cmake/Release').absolutePath)
	if( !folder.exists() ) {
		folder.mkdirs()
	}

	doLast {
		exec {
			workingDir 'benchmarks/ecoreBenchmark/.cmake/Release'
			commandLine 'cmd', '/c', 'cmake -G "MinGW Makefiles" -D CMAKE_BUILD_TYPE=Release ' + file("./benchmarks/ecoreBenchmark").absolutePath
		}
		exec {
			workingDir 'benchmarks/ecoreBenchmark/.cmake/Release'
			if (make_parallel_jobs)
			{			
				commandLine 'cmd', '/c', 'mingw32-make install -j' + make_parallel_jobs
			}
			else
			{
				commandLine 'cmd', '/c', 'mingw32-make install' 
			}
		}
	}
}

task compileMemoryBenchmarkEcore(dependsOn: ['compileMemoryBenchmarkEcoreDebug', 'compileMemoryBenchmarkEcoreRelease']) {
	group 'Examples'
	description 'compile memoryBenchmarkEcore in debug and release'
}
task compileMemoryBenchmarkEcoreDebug {
	group 'Examples'
	description 'compile memoryBenchmarkEcore in Debug'

	def folder = new File(file('./benchmarks/memoryBenchmarkEcore/.cmake/Debug').absolutePath)
	if( !folder.exists() ) {
		folder.mkdirs()
	}

	doLast {
		exec {
			workingDir 'benchmarks/memoryBenchmarkEcore/.cmake/Debug'
			commandLine 'cmd', '/c', 'cmake -G "MinGW Makefiles" -D CMAKE_BUILD_TYPE=Debug ' + file("./benchmarks/memoryBenchmarkEcore").absolutePath
		}
		exec {
			workingDir 'benchmarks/memoryBenchmarkEcore/.cmake/Debug'
			if (make_parallel_jobs)
			{			
				commandLine 'cmd', '/c', 'mingw32-make install -j' + make_parallel_jobs
			}
			else
			{
				commandLine 'cmd', '/c', 'mingw32-make install' 
			}
		}
	}
}
task compileMemoryBenchmarkEcoreRelease {
	group 'Examples'
	description 'compile memoryBenchmarkEcore in Release'

	def folder = new File(file('./benchmarks/memoryBenchmarkEcore/.cmake/Release').absolutePath)
	if( !folder.exists() ) {
		folder.mkdirs()
	}

	doLast {
		exec {
			workingDir 'benchmarks/memoryBenchmarkEcore/.cmake/Release'
			commandLine 'cmd', '/c', 'cmake -G "MinGW Makefiles" -D CMAKE_BUILD_TYPE=Release ' + file("./benchmarks/memoryBenchmarkEcore").absolutePath
		}
		exec {
			workingDir 'benchmarks/memoryBenchmarkEcore/.cmake/Release'
			if (make_parallel_jobs)
			{			
				commandLine 'cmd', '/c', 'mingw32-make install -j' + make_parallel_jobs
			}
			else
			{
				commandLine 'cmd', '/c', 'mingw32-make install' 
			}
		}
	}
}


task compileUMLBenchmark(dependsOn: ['compileUMLBenchmarkDebug', 'compileUMLBenchmarkRelease']) {
	group 'Examples'
	description 'compile UMLBenchmark in debug and release'
}
task compileUMLBenchmarkDebug {
	group 'Examples'
	description 'compile UMLBenchmark in Debug'

	def folder = new File(file('./benchmarks/umlBenchmark/.cmake/Debug').absolutePath)
	if( !folder.exists() ) {
		folder.mkdirs()
	}

	doLast {
		exec {
			workingDir 'benchmarks/umlBenchmark/.cmake/Debug'
			commandLine 'cmd', '/c', 'cmake -G "MinGW Makefiles" -D CMAKE_BUILD_TYPE=Debug ' + file("./benchmarks/umlBenchmark").absolutePath
		}
		exec {
			workingDir 'benchmarks/umlBenchmark/.cmake/Debug'
			if (make_parallel_jobs)
			{			
				commandLine 'cmd', '/c', 'mingw32-make install -j' + make_parallel_jobs
			}
			else
			{
				commandLine 'cmd', '/c', 'mingw32-make install' 
			}
		}
	}
}
task compileUMLBenchmarkRelease {
	group 'Examples'
	description 'compile UMLBenchmark in Release'

	def folder = new File(file('./benchmarks/umlBenchmark/.cmake/Release').absolutePath)
	if( !folder.exists() ) {
		folder.mkdirs()
	}

	doLast {
		exec {
			workingDir 'benchmarks/umlBenchmark/.cmake/Release'
			commandLine 'cmd', '/c', 'cmake -G "MinGW Makefiles" -D CMAKE_BUILD_TYPE=Release ' + file("./benchmarks/umlBenchmark").absolutePath
		}
		exec {
			workingDir 'benchmarks/umlBenchmark/.cmake/Release'
			if (make_parallel_jobs)
			{			
				commandLine 'cmd', '/c', 'mingw32-make install -j' + make_parallel_jobs
			}
			else
			{
				commandLine 'cmd', '/c', 'mingw32-make install' 
			}
		}
	}
}
