// DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN
// @generator: ecore4cpp::generator::main::generateBuildFile
plugins{
	id "tui.sse.mde4cpp.MDE4CPPGenerate" version "0.6"	
}

import org.gradle.internal.os.OperatingSystem;

description 'Build task for project: subsetUnion'


task runCommandCompileSubsetUnion(type:Exec) {
	workingDir project.projectDir
	def rootPath = project.rootDir.toPath()
	def projectPath = project.projectDir.toPath()
	def relativeToRoot = rootPath.relativize(projectPath)
	def depth = relativeToRoot.nameCount
	def gradlewRelPath = '../' * depth + 'application/tools/gradlew'
	if (OperatingSystem.current().isWindows()) {
		def gradlewPathWin = gradlewRelPath.replace('/', '\\')
		commandLine 'cmd', '/c', gradlewPathWin + '.bat src_gen:compileSubsetUnionSrc'
	} else {
		commandLine '/bin/sh', '-c', gradlewRelPath + ' src_gen:compileSubsetUnionSrc'
	}
	dependsOn 'generateSubsetUnion'
	dependsOn ':src:ecore:buildEcore'
		def allDepOutputs = files()
		getRootProject().getTasksByName('buildEcore', true).each { task ->
			allDepOutputs.from(task.outputs.files)
		}
		inputs.files(allDepOutputs)
		mustRunAfter = [
			'generateSubsetUnion'
			, getRootProject().getTasksByName('compileEcore', true)
			, getRootProject().getTasksByName('buildEcore', true)
		]
	doFirst {
		def mde4cppHome = System.getenv('MDE4CPP_HOME')
		if (mde4cppHome && OperatingSystem.current().isLinux()) {
			def ecoreLibFile = new File(mde4cppHome, 'application/bin/ecore.so')
			if (!ecoreLibFile.exists()) {
				sleep(2000)
				if (!ecoreLibFile.exists()) {
					def taskPath = ':src:ecore:buildEcore'
					throw new GradleException("Dependency library file does not exist: " + ecoreLibFile.absolutePath + ". Task " + taskPath + " must complete first.")
				}
			}
		}
	}
}

task runCommandGenerateSubsetUnion(type: tui.sse.mde4cpp.MDE4CPPGenerate) {
	def modelPath = file('.' + File.separator + 'model' + File.separator + 'subsetUnion.ecore')
	modelFilePath = modelPath.absolutePath
}

task runCommandCompileApplicationForSubsetUnion(type:Exec) {
	workingDir project.projectDir
	def rootPath = project.rootDir.toPath()
	def projectPath = project.projectDir.toPath()
	def relativeToRoot = rootPath.relativize(projectPath)
	def depth = relativeToRoot.nameCount
	def gradlewRelPath = '../' * depth + 'application/tools/gradlew'
	if (OperatingSystem.current().isWindows()) {
		def gradlewPathWin = gradlewRelPath.replace('/', '\\')
		commandLine 'cmd', '/c', gradlewPathWin + '.bat application:compileApplicationForSubsetUnionSrc'
	} else {
		commandLine '/bin/sh', '-c', gradlewRelPath + ' application:compileApplicationForSubsetUnionSrc'
	}
	dependsOn 'compileSubsetUnion'
}

task generateSubsetUnion() {
	group 'subsetUnion'	
	description 'generate C++ code of subsetUnion.ecore model subsetUnion' 
 
	def buildFile = file('model'+ File.separator +'build.gradle')
	if(!buildFile.exists())
	{
		dependsOn 'runCommandGenerateSubsetUnion'
	}
	else
	{
		dependsOn 'model:generateSubsetUnionModel'
	}
}

task compileSubsetUnion() {
	group 'subsetUnion'
	description 'compile subsetUnion' 

	dependsOn 'generateSubsetUnion'
	mustRunAfter = ["generateSubsetUnion"]
	
	def buildFile = file('src_gen'+ File.separator + 'build.gradle')
	
	if(buildFile.exists()) {
		dependsOn 'src_gen:compileSubsetUnionSrc'
	} else {
		dependsOn 'runCommandCompileSubsetUnion'
	}
	
		mustRunAfter = [
			'generateSubsetUnion'
			, getRootProject().getTasksByName('compileEcore', true)
			, getRootProject().getTasksByName('buildEcore', true)
		]
}

task compileApplicationForSubsetUnion{
	group 'subsetUnion'
	description 'compile ApplicationForsubsetUnion'

	dependsOn 'runCommandCompileApplicationForSubsetUnion'
	dependsOn 'compileSubsetUnion'
	mustRunAfter = ["compileSubsetUnion"]
}

task buildSubsetUnion() {
	group 'subsetUnion'
	description 'build subsetUnion'

	dependsOn 'generateSubsetUnion'
	dependsOn 'compileSubsetUnion'
	dependsOn 'compileApplicationForSubsetUnion'

	dependsOn getRootProject().getTasksByName('buildEcore', true)
}
