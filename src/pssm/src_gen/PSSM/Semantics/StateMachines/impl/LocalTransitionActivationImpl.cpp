
#include "PSSM/Semantics/StateMachines/impl/LocalTransitionActivationImpl.hpp"
#ifdef NDEBUG
	#define DEBUG_INFO(a)		/**/
	#define DEBUG_WARNING(a)	/**/
	#define DEBUG_ERROR(a)		/**/
#else
	#define DEBUG_INFO(a) 		std::cout<<"[\e[0;32mInfo\e[0m]:\t\t"<<__PRETTY_FUNCTION__<<"\n\t\t  -- Message: "<<a<<std::endl;
	#define DEBUG_WARNING(a) 	std::cout<<"[\e[0;33mWarning\e[0m]:\t"<<__PRETTY_FUNCTION__<<"\n\t\t  -- Message: "<<a<<std::endl;
	#define DEBUG_ERROR(a)		std::cout<<"[\e[0;31mError\e[0m]:\t"<<__PRETTY_FUNCTION__<<"\n\t\t  -- Message: "<<a<<std::endl;
#endif

#ifdef ACTIVITY_DEBUG_ON
    #define ACT_DEBUG(a) a
#else
    #define ACT_DEBUG(a) /**/
#endif

//#include "util/ProfileCallCount.hpp"

#include <cassert>
#include <iostream>
#include <sstream>

#include "abstractDataTypes/Bag.hpp"


#include "ecore/EcoreAny.hpp"
#include "ecore/EcoreContainerAny.hpp"
#include "abstractDataTypes/SubsetUnion.hpp"
#include "ecore/EAnnotation.hpp"
#include "ecore/EClass.hpp"
#include "ecore/EAttribute.hpp"
#include "ecore/EReference.hpp"
#include "ecore/EStructuralFeature.hpp"
#include "ecore/ecorePackage.hpp"
#include "ecore/ecoreFactory.hpp"
//Includes from codegen annotation
#include "PSSM/Semantics/StateMachines/impl/EntryPointPseudostateActivationImpl.hpp"
#include "uml/Vertex.hpp"
#include "PSSM/Semantics/StateMachines/EntryPointPseudostateActivation.hpp"
#include "PSSM/Semantics/StateMachines/StateMachineExecution.hpp"
#include "PSSM/Semantics/StateMachines/StateMachineConfiguration.hpp"
//Forward declaration includes
#include "persistence/interfaces/XLoadHandler.hpp" // used for Persistence
#include "persistence/interfaces/XSaveHandler.hpp" // used for Persistence

#include <exception> // used in Persistence
#include "fUML/Semantics/Loci/LociFactory.hpp"
#include "uml/umlFactory.hpp"
#include "PSSM/Semantics/StateMachines/StateMachinesFactory.hpp"
#include "fUML/Semantics/CommonBehavior/CommonBehaviorFactory.hpp"
#include "fUML/Semantics/CommonBehavior/EventOccurrence.hpp"
#include "uml/NamedElement.hpp"
#include "PSSM/Semantics/StateMachines/RegionActivation.hpp"
#include "fUML/Semantics/Loci/SemanticVisitor.hpp"
#include "PSSM/Semantics/StateMachines/StateActivation.hpp"
#include "PSSM/Semantics/StateMachines/TransitionActivation.hpp"
#include "PSSM/Semantics/StateMachines/VertexActivation.hpp"
//Factories and Package includes
#include "PSSM/PSSMPackage.hpp"
#include "PSSM/Semantics/SemanticsPackage.hpp"
#include "fUML/Semantics/CommonBehavior/CommonBehaviorPackage.hpp"
#include "fUML/Semantics/Loci/LociPackage.hpp"
#include "PSSM/Semantics/StateMachines/StateMachinesPackage.hpp"
#include "uml/umlPackage.hpp"

using namespace PSSM::Semantics::StateMachines;

//*********************************
// Constructor / Destructor
//*********************************
LocalTransitionActivationImpl::LocalTransitionActivationImpl()
{	
	/*
	NOTE: Due to virtual inheritance, base class constrcutors may not be called correctly
	*/
}

LocalTransitionActivationImpl::~LocalTransitionActivationImpl()
{
#ifdef SHOW_DELETION
	std::cout << "-------------------------------------------------------------------------------------------------\r\ndelete LocalTransitionActivation "<< this << "\r\n------------------------------------------------------------------------ " << std::endl;
#endif
}


LocalTransitionActivationImpl::LocalTransitionActivationImpl(const LocalTransitionActivationImpl & obj): LocalTransitionActivationImpl()
{
	*this = obj;
}

LocalTransitionActivationImpl& LocalTransitionActivationImpl::operator=(const LocalTransitionActivationImpl & obj)
{
	//call overloaded =Operator for each base class
	TransitionActivationImpl::operator=(obj);
	/* TODO: Find out if this call is necessary
	 * Currently, this causes an error because it calls an implicit assignment operator of LocalTransitionActivation 
	 * which is generated by the compiler (as LocalTransitionActivation is an abstract class and does not have a user-defined assignment operator).
	 * Implicit compiler-generated assignment operators however only create shallow copies of members,
	 * which implies, that not a real deep copy is created when using the copy()-method.
	 * 
	 * NOTE: Since all members are deep-copied by this assignment-operator anyway, why is it even necessary to call this implicit assignment-operator?
	 * This is only done for ecore-models, not for UML-models.
	 */
	//LocalTransitionActivation::operator=(obj);

	//create copy of all Attributes
	#ifdef SHOW_COPIES
	std::cout << "+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++\r\ncopy LocalTransitionActivation "<< this << "\r\n+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ " << std::endl;
	#endif
	//Clone Attributes with (deep copy)

	//copy references with no containment (soft copy)
	//Clone references with containment (deep copy)
	return *this;
}

std::shared_ptr<ecore::EObject> LocalTransitionActivationImpl::copy() const
{
	std::shared_ptr<LocalTransitionActivationImpl> element(new LocalTransitionActivationImpl());
	*element =(*this);
	element->setThisLocalTransitionActivationPtr(element);
	return element;
}

//*********************************
// Operations
//*********************************
void LocalTransitionActivationImpl::enterTarget(const std::shared_ptr<fUML::Semantics::CommonBehavior::EventOccurrence>& eventOccurrence)
{
	//ADD_COUNT(__PRETTY_FUNCTION__)
	//generated from body annotation
	// Entering the target of LocalTransition consists in checking if the target can be entered. If
	// this is the case then only when the target is not also the containing State it is entered
	if (this->getTargetVertexActivation()->isEnterable(this->getThisTransitionActivationPtr(), false))
	{	
		if (this->getTargetVertexActivation() != this->getContainingState()){
			this->getTargetVertexActivation()->enter(this->getThisTransitionActivationPtr(), eventOccurrence, this->getLeastCommonAncestor());
		}
	}
	//end of body
}

void LocalTransitionActivationImpl::exitSource(const std::shared_ptr<fUML::Semantics::CommonBehavior::EventOccurrence>& eventOccurrence)
{
	//ADD_COUNT(__PRETTY_FUNCTION__)
	//generated from body annotation
	// Exiting the source State of a LocalTransition consists in the following set of actions:
	// 1 - Check if the source State can be exited (if it cannot then do nothing)
	// 2 - If the source can be exited and this latter is an entry point then the exit
	//     of the source is trivial (i.e., it only consists in exiting the entry point)
	//   - If the source can be exited and this latter is a composite State then the top
	//     vertex that is located in the top Region which contains (maybe in deep nesting)
	//     the target is exited. At this point, if the source is not the containing State
	//     then it is also exited
	const auto& containingStateActivation = this->getContainingState();
	if (this->getSourceVertexActivation()->isExitable(this->getThisTransitionActivationPtr(), false)){
		if(std::dynamic_pointer_cast<PSSM::Semantics::StateMachines::EntryPointPseudostateActivation>(this->getSourceVertexActivation()))
		{
			this->getSourceVertexActivation()->exit(this->getThisTransitionActivationPtr(), eventOccurrence, nullptr);
		}
		else
		{
			for (const auto& containingStateRegionActivation : *containingStateActivation->getRegionActivation())
			{
				if (containingStateRegionActivation->getVertexActivation(std::dynamic_pointer_cast<uml::Vertex>(this->getSourceVertexActivation()->getNode())))
				{
					for (const auto& containingStateRegionVertexActivation : *containingStateRegionActivation->getVertexActivations())
					{
						if (this->getStateMachineExecution()->getConfiguration()->isActive(containingStateRegionVertexActivation))
						{
							containingStateRegionVertexActivation->exit(this->getThisTransitionActivationPtr(), eventOccurrence, nullptr);
						}
					}
				}
			}
			if (this->getSourceVertexActivation() != containingStateActivation)
			{
				this->getSourceVertexActivation()->exit(this->getThisTransitionActivationPtr(), eventOccurrence, this->getLeastCommonAncestor());
			}
		}
	}
	//end of body
}

std::shared_ptr<PSSM::Semantics::StateMachines::StateActivation> LocalTransitionActivationImpl::getContainingState()
{
	//ADD_COUNT(__PRETTY_FUNCTION__)
	//generated from body annotation
	// The container of a LocalTransition is determined in the following manner:
	// 1 - If the source Vertex is an EntryPoint, then the containing State is the
	//     the State that has this EntryPoint on its edge
	// 2 - Else if the source contains the target, then the containing
	//	   State is the source itself. Otherwise the source is the target
	auto containingState = std::shared_ptr<PSSM::Semantics::StateMachines::StateActivation>();
	if (auto PSActivation = std::dynamic_pointer_cast<PSSM::Semantics::StateMachines::EntryPointPseudostateActivation>(this->getSourceActivation()))
	{
		containingState = std::dynamic_pointer_cast<PSSM::Semantics::StateMachines::StateActivation>(this->getSourceActivation()->getParentVertexActivation());
	}
	else
	{
		if (this->getSourceActivation()->getVertexActivation(std::dynamic_pointer_cast<uml::Vertex>(this->getTargetActivation()->getNode())))
		{
			containingState = std::dynamic_pointer_cast<PSSM::Semantics::StateMachines::StateActivation>(this->getSourceActivation());
		}
		else
		{
			containingState = std::dynamic_pointer_cast<PSSM::Semantics::StateMachines::StateActivation>(this->getTargetActivation());
		}
	}
	return containingState;
	//end of body
}

//*********************************
// Attribute Getters & Setters
//*********************************

//*********************************
// Reference Getters & Setters
//*********************************

//*********************************
// Union Getter
//*********************************

//*********************************
// Container Getter
//*********************************
std::shared_ptr<ecore::EObject> LocalTransitionActivationImpl::eContainer() const
{
	return nullptr;
}

//*********************************
// Persistence Functions
//*********************************
void LocalTransitionActivationImpl::load(std::shared_ptr<persistence::interfaces::XLoadHandler> loadHandler)
{
	std::map<std::string, std::string> attr_list = loadHandler->getAttributeList();
	loadAttributes(loadHandler, attr_list);

	//
	// Create new objects (from references (containment == true))
	//
	// get PSSMFactory
	int numNodes = loadHandler->getNumOfChildNodes();
	for(int ii = 0; ii < numNodes; ii++)
	{
		loadNode(loadHandler->getNextNodeName(), loadHandler);
	}
}		

void LocalTransitionActivationImpl::loadAttributes(std::shared_ptr<persistence::interfaces::XLoadHandler> loadHandler, std::map<std::string, std::string> attr_list)
{

	TransitionActivationImpl::loadAttributes(loadHandler, attr_list);
}

void LocalTransitionActivationImpl::loadNode(std::string nodeName, std::shared_ptr<persistence::interfaces::XLoadHandler> loadHandler)
{

	//load BasePackage Nodes
	TransitionActivationImpl::loadNode(nodeName, loadHandler);
}

void LocalTransitionActivationImpl::resolveReferences(const int featureID, std::vector<std::shared_ptr<ecore::EObject>> references)
{
	TransitionActivationImpl::resolveReferences(featureID, references);
}

void LocalTransitionActivationImpl::save(std::shared_ptr<persistence::interfaces::XSaveHandler> saveHandler) const
{
	saveContent(saveHandler);

	TransitionActivationImpl::saveContent(saveHandler);
	
	StateMachineSemanticVisitorImpl::saveContent(saveHandler);
	
	fUML::Semantics::Loci::SemanticVisitorImpl::saveContent(saveHandler);
	
	ecore::EObjectImpl::saveContent(saveHandler);
}

void LocalTransitionActivationImpl::saveContent(std::shared_ptr<persistence::interfaces::XSaveHandler> saveHandler) const
{
	try
	{
		std::shared_ptr<PSSM::Semantics::StateMachines::StateMachinesPackage> package = PSSM::Semantics::StateMachines::StateMachinesPackage::eInstance();
	}
	catch (std::exception& e)
	{
		std::cout << "| ERROR    | " << e.what() << std::endl;
	}
}

const std::shared_ptr<ecore::EClass>& LocalTransitionActivationImpl::eStaticClass() const
{
	return PSSM::Semantics::StateMachines::StateMachinesPackage::eInstance()->getLocalTransitionActivation_Class();
}

//*********************************
// EStructuralFeature Get/Set/IsSet
//*********************************
std::shared_ptr<Any> LocalTransitionActivationImpl::eGet(int featureID, bool resolve, bool coreType) const
{
	switch(featureID)
	{
	}
	return TransitionActivationImpl::eGet(featureID, resolve, coreType);
}

bool LocalTransitionActivationImpl::internalEIsSet(int featureID) const
{
	switch(featureID)
	{
	}
	return TransitionActivationImpl::internalEIsSet(featureID);
}

bool LocalTransitionActivationImpl::eSet(int featureID,  const std::shared_ptr<Any>& newValue)
{
	switch(featureID)
	{
	}

	return TransitionActivationImpl::eSet(featureID, newValue);
}

//*********************************
// EOperation Invoke
//*********************************
std::shared_ptr<Any> LocalTransitionActivationImpl::eInvoke(int operationID, const std::shared_ptr<Bag<Any>>& arguments)
{
	std::shared_ptr<Any> result;
 
  	switch(operationID)
	{
		// PSSM::Semantics::StateMachines::LocalTransitionActivation::enterTarget(fUML::Semantics::CommonBehavior::EventOccurrence): 3208005632
		case StateMachinesPackage::LOCALTRANSITIONACTIVATION_OPERATION_ENTERTARGET_EVENTOCCURRENCE:
		{
			//Retrieve input parameter 'eventOccurrence'
			//parameter 0
			std::shared_ptr<fUML::Semantics::CommonBehavior::EventOccurrence> incoming_param_eventOccurrence;
			Bag<Any>::const_iterator incoming_param_eventOccurrence_arguments_citer = std::next(arguments->begin(), 0);
			{
				std::shared_ptr<ecore::EcoreAny> ecoreAny = std::dynamic_pointer_cast<ecore::EcoreAny>((*incoming_param_eventOccurrence_arguments_citer));
				if(ecoreAny)
				{
					try
					{
						std::shared_ptr<ecore::EObject> _temp = ecoreAny->getAsEObject();
						incoming_param_eventOccurrence = std::dynamic_pointer_cast<fUML::Semantics::CommonBehavior::EventOccurrence>(_temp);
					}
					catch(...)
					{
						DEBUG_ERROR("Invalid type stored in 'ecore::EcoreAny' for parameter 'eventOccurrence'. Failed to invoke operation 'enterTarget'!")
						return nullptr;
					}
				}
				else
				{
					DEBUG_ERROR("Invalid instance of 'ecore::EcoreAny' for parameter 'eventOccurrence'. Failed to invoke operation 'enterTarget'!")
					return nullptr;
				}
			}
		
			this->enterTarget(incoming_param_eventOccurrence);
			break;
		}
		// PSSM::Semantics::StateMachines::LocalTransitionActivation::exitSource(fUML::Semantics::CommonBehavior::EventOccurrence): 1127739646
		case StateMachinesPackage::LOCALTRANSITIONACTIVATION_OPERATION_EXITSOURCE_EVENTOCCURRENCE:
		{
			//Retrieve input parameter 'eventOccurrence'
			//parameter 0
			std::shared_ptr<fUML::Semantics::CommonBehavior::EventOccurrence> incoming_param_eventOccurrence;
			Bag<Any>::const_iterator incoming_param_eventOccurrence_arguments_citer = std::next(arguments->begin(), 0);
			{
				std::shared_ptr<ecore::EcoreAny> ecoreAny = std::dynamic_pointer_cast<ecore::EcoreAny>((*incoming_param_eventOccurrence_arguments_citer));
				if(ecoreAny)
				{
					try
					{
						std::shared_ptr<ecore::EObject> _temp = ecoreAny->getAsEObject();
						incoming_param_eventOccurrence = std::dynamic_pointer_cast<fUML::Semantics::CommonBehavior::EventOccurrence>(_temp);
					}
					catch(...)
					{
						DEBUG_ERROR("Invalid type stored in 'ecore::EcoreAny' for parameter 'eventOccurrence'. Failed to invoke operation 'exitSource'!")
						return nullptr;
					}
				}
				else
				{
					DEBUG_ERROR("Invalid instance of 'ecore::EcoreAny' for parameter 'eventOccurrence'. Failed to invoke operation 'exitSource'!")
					return nullptr;
				}
			}
		
			this->exitSource(incoming_param_eventOccurrence);
			break;
		}
		// PSSM::Semantics::StateMachines::LocalTransitionActivation::getContainingState() : PSSM::Semantics::StateMachines::StateActivation: 790684891
		case StateMachinesPackage::LOCALTRANSITIONACTIVATION_OPERATION_GETCONTAININGSTATE:
		{
			result = eEcoreAny(this->getContainingState(), PSSM::Semantics::StateMachines::StateMachinesPackage::STATEACTIVATION_CLASS);
			break;
		}

		default:
		{
			// call superTypes
			result = TransitionActivationImpl::eInvoke(operationID, arguments);
			if (result && !result->isEmpty())
				break;
			break;
		}
  	}

	return result;
}

std::shared_ptr<PSSM::Semantics::StateMachines::LocalTransitionActivation> LocalTransitionActivationImpl::getThisLocalTransitionActivationPtr() const
{
	return m_thisLocalTransitionActivationPtr.lock();
}
void LocalTransitionActivationImpl::setThisLocalTransitionActivationPtr(std::weak_ptr<PSSM::Semantics::StateMachines::LocalTransitionActivation> thisLocalTransitionActivationPtr)
{
	m_thisLocalTransitionActivationPtr = thisLocalTransitionActivationPtr;
	setThisTransitionActivationPtr(thisLocalTransitionActivationPtr);
}


