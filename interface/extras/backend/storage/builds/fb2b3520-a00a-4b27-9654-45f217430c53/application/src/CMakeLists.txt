# DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN
# @generator: ecore4cpp::generator::main::generateApplication

# C++ project of application for copyTest.ecore, generated by UML4CPP

CMAKE_MINIMUM_REQUIRED(VERSION 3.9)

# Start of user code project_name
# You may manually edit the project name, won't be overwritten upon generation.
PROJECT(App_copyTest)
# End of user code

IF(NOT CMAKE_BUILD_TYPE) 
    SET(CMAKE_BUILD_TYPE Debug)
ENDIF(NOT CMAKE_BUILD_TYPE)

SET(CMAKE_CXX_STANDARD 17)

SET(CMAKE_CXX_FLAGS "-Wall -Wno-overloaded-virtual -Wdeprecated-declarations -fmax-errors=5")
IF(APPLE)
  string(APPEND CMAKE_CXX_FLAGS " -Qunused-arguments")
ENDIF(APPLE)
SET(CMAKE_CXX_FLAGS_DEBUG " -Og -ggdb -DACTIVITY_DEBUG_ON")
SET(CMAKE_CXX_FLAGS_RELEASE " -O3 -DNDEBUG")

string(REPLACE "\\" "/" MDE4CPP_HOME $ENV{MDE4CPP_HOME})

SET(SOURCE_FILES
	main.cpp
# Start of user code cpp
# You may manually edit the following lines, won't be overwritten upon generation.

# End of user code
)

INCLUDE_DIRECTORIES(
	../
	${MDE4CPP_HOME}/application/include

# Start of user code includes
# You may manually edit the following lines, won't be overwritten upon generation.

# End of user code
)

# Apple specific stuff
IF(APPLE)
  SET(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} -undefined dynamic_lookup")
  SET(CMAKE_MODULE_LINKER_FLAGS "${CMAKE_MODULE_LINKER_FLAGS} -undefined dynamic_lookup")
ENDIF(APPLE)

IF(UNIX AND NOT APPLE)
    # for Linux, BSD, Solaris, Minix
	IF (CMAKE_BUILD_TYPE STREQUAL "Debug")
		FIND_LIBRARY(ECORE_DEBUG ecored.so ${MDE4CPP_HOME}/application/bin)
		FIND_LIBRARY(COPYTEST_DEBUG copyTestd.so ${MDE4CPP_HOME}/application/bin)
		# Start of user code library_Linux_debug 
		# You may manually edit the following lines, won't be overwritten upon generation.
		
		# End of user code
	ELSE()
		FIND_LIBRARY(ECORE_RELEASE ecore.so ${MDE4CPP_HOME}/application/bin)
		FIND_LIBRARY(COPYTEST_RELEASE copyTest.so ${MDE4CPP_HOME}/application/bin)
		# Start of user code library_Linux_optimized 
		# You may manually edit the following lines, won't be overwritten upon generation.
		
		# End of user code
	ENDIF()
ELSEIF(APPLE)
	IF (CMAKE_BUILD_TYPE STREQUAL "Debug")
		FIND_LIBRARY(ECORE_DEBUG ecored.dylib ${MDE4CPP_HOME}/application/bin)
		FIND_LIBRARY(COPYTEST_DEBUG copyTestd.dylib ${MDE4CPP_HOME}/application/bin)
		# Start of user code library_Apple_debug 
		# You may manually edit the following lines, won't be overwritten upon generation.
		
		# End of user code
	ELSE()
		FIND_LIBRARY(ECORE_RELEASE ecore.dylib ${MDE4CPP_HOME}/application/bin)
		FIND_LIBRARY(COPYTEST_RELEASE copyTest.dylib ${MDE4CPP_HOME}/application/bin)
		# Start of user code library_Apple_optimized 
		# You may manually edit the following lines, won't be overwritten upon generation.
		
		# End of user code
	ENDIF()
ELSE()
	IF (CMAKE_BUILD_TYPE STREQUAL "Debug")
		FIND_LIBRARY(ECORE_DEBUG ecored ${MDE4CPP_HOME}/application/lib)
		FIND_LIBRARY(COPYTEST_DEBUG copyTestd ${MDE4CPP_HOME}/application/lib)
		# Start of user code library_Win_debug 
		# You may manually edit the following lines, won't be overwritten upon generation.
		
		# End of user code
	ELSE()
		FIND_LIBRARY(ECORE_RELEASE ecore ${MDE4CPP_HOME}/application/lib)
		FIND_LIBRARY(COPYTEST_RELEASE copyTest ${MDE4CPP_HOME}/application/lib)
		# Start of user code library_Win_optimized 
		# You may manually edit the following lines, won't be overwritten upon generation.
		
		# End of user code
	ENDIF()
ENDIF()

ADD_EXECUTABLE(${PROJECT_NAME} ${SOURCE_FILES})
SET_TARGET_PROPERTIES(${PROJECT_NAME} PROPERTIES DEBUG_POSTFIX "d")

IF (CMAKE_BUILD_TYPE STREQUAL "Debug")
	TARGET_LINK_LIBRARIES(${PROJECT_NAME}
		debug ${ECORE_DEBUG}
		debug ${COPYTEST_DEBUG}
	
	
		# Start of user code library_linking_debug 
		# You may manually edit the following lines, won't be overwritten upon generation.
	
		# End of user code
	)
ELSE()
	TARGET_LINK_LIBRARIES(${PROJECT_NAME}
		optimized ${ECORE_RELEASE}
		optimized ${COPYTEST_RELEASE}
	
	
		# Start of user code library_linking_optimized 
		# You may manually edit the following lines, won't be overwritten upon generation.
	
		# End of user code
	)
ENDIF()

#-----------------------------------------------------
# DELIVERING binaries and header to ${MDE4CPP_HOME}
#-----------------------------------------------------

INSTALL(TARGETS ${PROJECT_NAME} 
	RUNTIME DESTINATION ${MDE4CPP_HOME}/application/bin
)
